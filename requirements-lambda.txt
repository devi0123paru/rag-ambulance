# ⚡ LAMBDA-OPTIMIZED DEPENDENCIES (targets <200MB)
# Removes: sentence-transformers, chromadb, torch, sklearn
# Uses: API-based embeddings, OpenSearch/DynamoDB instead

# LLM & RAG core
langchain==0.3.0
langchain-community==0.3.0
langchain-core==0.3.0
langchain-groq==0.2.0
langchain-openai==0.2.0

# API & async
fastapi==0.115.0
uvicorn==0.28.0
pydantic==2.9.0
pydantic-settings==2.2.1
python-multipart==0.0.12
sse-starlette==2.1.0
mangum==0.18.0

# Utilities
python-dotenv==1.0.1
httpx==0.27.0
pypdf==5.0.0

# Optional: Lightweight vector DB (only if using DynamoDB instead of Chroma)
# dynamodb==1.0.0  # OR use boto3

# ❌ DO NOT INCLUDE (will break 500MB limit):
# - sentence-transformers (400MB+)
# - chromadb (100-200MB)  
# - torch (500MB+)
# - scikit-learn (50MB+)
